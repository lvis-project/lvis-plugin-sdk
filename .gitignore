node_modules/
*.log
.omc/
.host/
package-lock.json
storybook-static/
# Legacy binary lockfile from older bun versions; we standardize on text bun.lock.
bun.lockb
# dist/ is committed to Git so GitHub-hosted consumers (github:org/repo#tag)
# get pre-built artifacts without relying on a prepare script.
# bun does not run prepare scripts for GitHub dependencies (security policy).
